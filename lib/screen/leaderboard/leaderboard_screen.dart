import 'package:flutter/material.dart';

/// PLACEHOLDER — thay bằng LeaderboardScreen thật của bạn.
/// Giữ nguyên tên file (`leaderboard_screen.dart`) và tên class
/// (`LeaderboardScreen`) để không phải sửa import trong `social_screen.dart`.
class LeaderboardScreen extends StatelessWidget {
  const LeaderboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Xếp Hạng (chưa triển khai)'));
  }
}
